# Function with Multiple Returns
function min_max(arr):
    min = arr[0]
    max = arr[0]
    
    for item in arr:
        if item < min:
            min = item
        if item > max:
            max = item
    
    return min
    return max
