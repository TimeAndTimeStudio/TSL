# Object with Method
function create_point(x, y):
    return { x: x, y: y }

function point_distance(p1, p2):
    dx = p2.x - p1.x
    dy = p2.y - p1.y
    return dx * dx + dy * dy

p1 = create_point(0, 0)
p2 = create_point(3, 4)
print(point_distance(p1, p2))
